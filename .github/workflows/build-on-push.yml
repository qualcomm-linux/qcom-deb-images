name: Build on push to branch

on:
  push:
    branches: [main]

# grant nothing by default; every job below declares the scopes it needs
permissions: {}

jobs:
  build:
    name: "Build (suite: ${{ matrix.suite }})"
    strategy:
      fail-fast: false
      matrix:
        suite: [trixie, forky]
    uses: ./.github/workflows/debos.yml
    permissions:
      contents: read
    with:
      suite: ${{ matrix.suite }}
      # QLI targets qcom-next; consume the debs published to EFS by the
      # "qcom-next linux build" workflow rather than a kernel from an
      # APT repository
      kernelpackages: efs/qcom-next
  schema-check:
    uses: ./.github/workflows/lava-schema-check.yml
    permissions:
      contents: read
  test:
    name: "Test (suite: ${{ matrix.suite }})"
    uses: ./.github/workflows/lava-test.yml
    needs: [build, schema-check]
    permissions:
      checks: write
      contents: read
      packages: read
      pull-requests: write
    secrets:
      LAVATOKEN: ${{ secrets.LAVATOKEN }}
    strategy:
      fail-fast: false
      # Keep this matrix in sync with the build matrix above; see #614.
      matrix:
        suite: [trixie, forky]
    with:
      # every leg of the build matrix uploads to the same S3 directory,
      # which debos.yml keys on the run id and attempt alone, so any leg's
      # URL reaches all of the images; the artifact prefix, not the URL,
      # selects which one to boot.
      url: ${{ needs.build.outputs.artifacts_url }}
      suite: ${{ matrix.suite }}
      # https://github.com/qualcomm-linux/qcom-deb-images/issues/537
      boards_exclude: '["monaco-arduino-monza", "qrb2210-arduino-imola"]'
